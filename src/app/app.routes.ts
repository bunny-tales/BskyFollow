import { Routes } from '@angular/router';
import { UnfollowersComponent } from './unfollowers/unfollowers.component';

export const routes: Routes = [
    {
        path: '',
        title:'Home',
        component: UnfollowersComponent
    }
];
